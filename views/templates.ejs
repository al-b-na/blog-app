<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container py-4">
        <h1 class="display-5">Your Posts</h1>

        <!-- Check if there are any posts -->
        <% if (posts.length===0) { %>
            <p>No posts available. Add a new post to get started!</p>
            <% } else { %>
                <div class="row row-cols-1 row-cols-md-3 g-4">
                    <!-- Loop through each post and render it -->
                    <% posts.forEach((post, index)=> { %>
                        <div class="col">
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">
                                        <%= post.title %>
                                    </h5>
                                    <p class="card-text">
                                        <%= post.date %>
                                    </p>
                                    <p class="card-text">
                                        <%= post.cardContent %>
                                    </p>
                                    <a href="/view/<%= index %>" class="btn btn-primary">View</a>
                                    <a href="/edit/<%= index %>" class="btn btn-secondary">Edit</a>
                                </div>
                            </div>
                        </div>
                        <% }) %>
                </div>
                <% } %>
    </div>
</body>

</html>